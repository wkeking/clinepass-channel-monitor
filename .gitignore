# 本机资料（需求文档 .reference/clinepass-channel-monitor-requirements.md、调研报告、实测响应、CPA 源码副本）——不随仓库发布
.reference/
# 兜底：需求文档若又被放回根目录，同样不发布
/clinepass-channel-monitor-requirements.md
# 本地 Go 工具链
.toolchain/
# 构建产物
dist/
*.so
*.h

# 例外：C ABI 声明头需要随仓库发布
!cdecl.h

# go build 产物（插件仓库根目录不应出现二进制）
/clinepass-channel-monitor
